// 동적 진입선 단일 소스 (2026-08-21 유저 확정, 풋살 전용):
// 랭킹류 지표의 소표본 컷은 고정값이 아니라 "그 지표의 표본 축 최대치 × ratio(기본 30%), 올림".
// 데이터가 쌓여 최대치가 오르면 진입선도 자동 상향. 시즌 극초반 소표본은 감수(고정 하한 없음).
export function dynamicMin(max, ratio = 0.3) {
  return Math.ceil((max || 0) * ratio);
}
